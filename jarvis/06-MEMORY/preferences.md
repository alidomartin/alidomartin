# Preferences

Jarvis reads this on every startup. Add stable preferences here as they
solidify. Examples:

- prefers metric units
- when chatting, keep replies under 3 sentences unless asked for a draft
- never publish content from a brief without explicit "ship it"
