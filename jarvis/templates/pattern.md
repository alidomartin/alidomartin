<%*
const t = tp.date.now("YYYY-MM-DD-HHmm");
await tp.file.rename(`${t}-pattern`);
-%>
---
created: <% tp.date.now("YYYY-MM-DD HH:mm") %>
type: pattern
tags: []
---

# <% tp.date.now("YYYY-MM-DD HH:mm") %> — Pattern

The principle:

Domain 1 (where I saw it first):

Domain 2 (where I saw it again):

Why this might be the same thing:
